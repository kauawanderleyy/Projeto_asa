[defaults]
inventory = hosts.ini
remote_user = vagrant
host_key_checking = False
# Trocamos ed25519 por rsa
private_key_file = /home/ifpb/.vagrant.d/insecure_private_keys/vagrant.key.rsa

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
